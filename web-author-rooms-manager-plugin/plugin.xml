<?xml version="1.0" encoding="UTF-8"?>
<!-- The plugin.dtd file is located in the OXYGEN_INSATALL_DIR/plugins directory -->
<!DOCTYPE plugin SYSTEM "../plugin.dtd">

<plugin class="ro.sync.exml.plugin.Plugin"

    id="web-author-rooms-manager-plugin"
    name="web-author-rooms-manager-plugin"
    description="Automatically open each document in a concurrent session. If two users open same document, they will be in the same room."
    version="${project.nosnapshot.version}"
    vendor="Syncro Soft">

  <runtime>
    <librariesFolder name="lib" />
    <library name="target/classes" />
    <librariesFolder name="target/lib" />
  </runtime>

  <extension type="WorkspaceAccess" class="com.oxygenxml.demo.roomsmanager.DocumentRoomAllocatorInstaller"/>
</plugin>
